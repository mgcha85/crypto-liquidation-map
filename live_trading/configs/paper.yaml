mode: paper
symbol: BTCUSDT
lookback_hours: 50
update_interval_sec: 3600
model_path: models/xgb_optuna_best.onnx
log_level: info
http_port: 8080

barrier:
  profit_take: 0.02
  stop_loss: 0.01
  horizon_hours: 48

position:
  size_pct: 0.10
  taker_fee_pct: 0.0004
  slippage_bps: 5.0
  leverage: 1

risk:
  daily_loss_limit_pct: 0.02
  weekly_loss_limit_pct: 0.05
  max_positions: 1

api:
  testnet: true
  timeout_sec: 30
